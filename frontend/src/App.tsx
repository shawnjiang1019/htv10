//import { useState } from 'react'
import './App.css'
import { DebateContainer } from './components/DebateContainer'

function App() {
  //const [showNewDebate, setShowNewDebate] = useState<boolean>(true);

  return (
   <DebateContainer />
  )
}

export default App